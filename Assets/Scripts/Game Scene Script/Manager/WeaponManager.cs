using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponManager : MonoBehaviour
{
    // Start is called before the first frame update
    //뒤에 무기 구현후 무기를 바꾸는 기능
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
